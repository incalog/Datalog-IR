module GraphTest

/*class Edge {
    val start: Node
    val end: Node

    def this(start: Node, end: Node) = {
        this.start = start
        this.end = end
    }
}

class Node {
    val label: String

    def this(label: String) = {
        this.label = label
    }
}

class Graph {
    var nodes: Set[Node]
    var edges: Set[Edge]

    def this(root: Node) = {
        this.nodes = Set(root)
        this.edges = Set[Edge]()
    }

    def add(from: Node, to: Node): Unit = {
        this.nodes = this.nodes ++ Set(from, to)
        //this.edges = this.edges ++ Set(new Edge(from, to))
    }

    @main
    def main(): Set[Any] = {
        val root: Node = new Node("Root")
        val g: Graph = new Graph(root)

        val child: Node = new Node("Child 1")
        g.add(root, child)

        return g.edges
    }
}*/

class Graph {
    @main
    def main(): Set[String] = {
        return Set[String]()
    }
}