module SetTest

class A {
  def this() = {}

  @main def main(): Set[(Int, Int, Int)] = {
    val as: Set[Int] = [-1, 1, 2]
    val bs: Set[Int] = [3, 4]
    val c: Int = 5
    return for (a <- as if (a < 2) && (a > 0); b <- bs)
      yield (a, b, c)
  }
}