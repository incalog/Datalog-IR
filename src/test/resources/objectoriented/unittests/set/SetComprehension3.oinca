module SetTest

class A {
  def this() = {}

  @main def main(): Set[(Int, Int)] = {
    // Body size: 4: return 1, 3; return 1, 4; return 2,3; return 2; 4
    return for (a <- Set(1,2); b <- Set(3,4)) yield (a, b)
  }
}