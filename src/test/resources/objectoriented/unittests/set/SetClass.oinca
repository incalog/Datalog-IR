module SetTest

class Num {
    var value: Any

    def this(value: Any) = {
        this.value = value
    }
}

class Integer extends Num {
    def this(value: `Int`) = {
        super(value)
    }
}

class Float extends Num {
    def this(value: `Double`) = {
        super(value)
    }
}

class A {
  def this() = {}

  def filter(as: Set[Num]): Set[Integer] = {
    return for (a <- as if instanceOf(a, Integer)) yield cast(a, Integer)
  }

  @main def main(): Set[Any] = {
    val a: A = new A()
    return for (v <- a.filter(Set(new Float(1.0), new Integer(10), new Integer(3)))) yield v.value
  }
}