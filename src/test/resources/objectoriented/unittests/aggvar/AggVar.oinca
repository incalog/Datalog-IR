module ReachabilityTest

class A {
    vag sum: Int = 0 with A.sum

    @static
    def sum(n1: Int, n2: Int): Int = {
        n1 + n2
    }

    @main
    def main(): Int = {
        val a: A = new A()
        a.sum #= 1
        a.sum #= 3
        a.sum #= 1
        a.sum
    }
}
