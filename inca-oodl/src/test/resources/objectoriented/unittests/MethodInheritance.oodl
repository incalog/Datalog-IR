module MethodInheritance

class A {
    def add(n: Int): Int = this.get() + n
    def get(): Int = 1
}

class B extends A {
    override def get(): Int = 2
}

class C extends B

@main
def main(): Int = {
    val c = new C()

    // should call: + A.add(B.get() +1) => 3
    c.add(1)
}