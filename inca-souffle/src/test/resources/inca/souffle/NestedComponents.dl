.comp A {
    .decl R(x: number)
    R(42).
}

.comp B {
    .decl Q(x: number)
    Q(41).

    .init a = A
    Q(x) :- a.R(x).
    //Q(x) :- b.a.R(x).
}

.init b = B

.decl S(x: number)
S(x) :- b.Q(x).

//.output config1.a.MagicNumber2
.output b.Q
.output S