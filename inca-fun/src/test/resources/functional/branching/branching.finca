module branching

@main def main(): Int =
    let x = true in
    let y = false in
    if (x)
        if (x && y)
            0
        else
            1
    else if (y)
        if (x && y)
            2
        else
            3
    else
        4