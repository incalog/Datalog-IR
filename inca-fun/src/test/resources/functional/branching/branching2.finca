module branching

@main def main(x: Boolean, y: Boolean): Int =
    if (x && (x && y))
        0
    else if (x && !(x && y))
        1
    else if ((!x && y) && (x && y))
        2
    else if ((!x && y) && !(x && y))
        3
    else
        4