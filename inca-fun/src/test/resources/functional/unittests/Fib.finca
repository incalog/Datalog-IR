module Fib
@main def main(n: Int): Int =
  fib(n)

def fib(n: Int): Int =
  if (n < 2)
    n
  else
    fib(n - 1) + fib(n - 2)

/*def fib(n: Int): Int =
  if (n == 0)
    0
  else if (n == 1)
    1
  else
    fib(n - 1) + fib(n - 2)*/
