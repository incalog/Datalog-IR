module Plus
data Nat = Zero() | Succ(Nat)

@FunctionalDependency(m -> Result)
def plus(m: Nat, n: Nat): Nat = m match {
  case Zero() => n
  case Succ(pred) => Succ(plus(pred, n))
}

@main def main(n: Nat): Nat =
  plus(n, Succ(Succ(Zero())))
